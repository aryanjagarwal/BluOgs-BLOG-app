/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'media.glamour.com', 'plus.unsplash.com', 'firebasestorage.googleapis.com'],
    },
}

module.exports = nextConfig
